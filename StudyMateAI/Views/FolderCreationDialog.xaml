<Window x:Class="StudyMateAI.Views.FolderCreationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yeni Klasör" Height="160" Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Klasör Adı:" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource MaterialDesignBody}"/>
            <TextBox x:Name="FolderNameTextBox" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,20"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="İptal" Click="CancelButton_Click" Margin="0,0,10,0" Style="{DynamicResource MaterialDesignOutlinedButton}"/>
                <Button Content="Oluştur" Click="OkButton_Click" Style="{DynamicResource NeonGlowButton}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
