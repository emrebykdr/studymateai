<Page x:Class="StudyMateAI.Views.CoursesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      TextElement.Foreground="{DynamicResource TextPrimaryBrush}"
      Title="Derslerim">
    
    <Page.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.3"
                                     EasingFunction="{StaticResource EaseOut}"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Course List -->
            <Border Grid.Column="0" Padding="32" CornerRadius="8" Background="{DynamicResource BgCardBrush}" BorderThickness="1" BorderBrush="{DynamicResource BgBorderBrush}">
                <StackPanel>
                    <Grid Margin="0,0,0,32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Derslerim" Style="{StaticResource H1}" Margin="0,0,0,4"/>
                            <TextBlock Text="Dönem derslerini yönet" Style="{StaticResource Subtitle}"/>
                        </StackPanel>

                        <!-- Add Course Button -->
                        <Button Grid.Column="1"
                                Content="YENİ DERS EKLE" 
                                Click="AddCourseButton_Click"
                                Style="{DynamicResource NeonGlowButton}"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Courses DataGrid -->
                    <DataGrid x:Name="CoursesDataGrid"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionChanged="CoursesDataGrid_SelectionChanged"
                              MaxHeight="500">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Ders Adı" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Kod" Binding="{Binding Code}" Width="100"/>
                            <DataGridTextColumn Header="Kredi" Binding="{Binding Credit}" Width="60"/>
                            <DataGridTextColumn Header="Vize" Binding="{Binding MidtermGrade}" Width="60"/>
                            <DataGridTextColumn Header="Gerekli Final" Binding="{Binding RequiredFinalGrade}" Width="120"/>
                            <DataGridTemplateColumn Header="İşlemler" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Düzenle" 
                                                    Style="{StaticResource MaterialDesignFlatButton}"
                                                    Foreground="{DynamicResource InfoBrush}"
                                                    Padding="8,4" Margin="0,0,8,0"
                                                    Click="EditCourseButton_Click"
                                                    Tag="{Binding Id}"/>
                                            <Button Content="Sil" 
                                                    Style="{StaticResource DangerButton}"
                                                    Padding="8,4"
                                                    Click="DeleteCourseButton_Click"
                                                    Tag="{Binding Id}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Right: Final Calculator -->
            <StackPanel Grid.Column="2">
                
                <!-- Calculator Card -->
                <Border Padding="32" CornerRadius="8" Margin="0,0,0,24" Background="{DynamicResource BgCardBrush}" BorderThickness="1" BorderBrush="{DynamicResource BgBorderBrush}">
                    <StackPanel>
                        <TextBlock Text="Final Hesaplayıcı" Style="{StaticResource H2}" Margin="0,0,0,4"/>
                        <TextBlock Text="Hedef notunu belirle" Style="{StaticResource Subtitle}" Margin="0,0,0,32"/>

                        <TextBlock Text="Ders Seç:" FontSize="13" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,8"/>
                        <TextBlock x:Name="SelectedCourseText" Text="Lütfen bir ders seçin" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,24"/>

                        <TextBlock Text="Vize Notu:" FontSize="13" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,8"/>
                        <TextBlock x:Name="MidtermGradeText" Text="-" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>

                        <Border Height="1" Background="{DynamicResource BgBorderBrush}" Margin="0,0,0,24"/>

                        <TextBlock Text="GEÇMEK İÇİN GEREKEN FİNAL NOTU" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,8"/>
                        <TextBlock x:Name="RequiredFinalText" Text="-" FontSize="42" FontWeight="Bold" Foreground="{DynamicResource WarningBrush}" Margin="0,0,0,12"/>

                        <TextBlock x:Name="PassStatusText" Text="" FontSize="13" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,24"/>

                        <Border Height="1" Background="{DynamicResource BgBorderBrush}" Margin="0,0,0,24"/>

                        <!-- Grade Scenarios -->
                        <TextBlock Text="Not Senaryoları:" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,16"/>

                        <StackPanel x:Name="GradeScenariosPanel">
                            <!-- Populated dynamically -->
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Info Box -->
                <Border Padding="16,20" CornerRadius="8" Background="{DynamicResource BgSurfaceBrush}" BorderBrush="{DynamicResource BgBorderBrush}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="InformationOutline" Width="20" Height="20" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Formül: (Vize × %40) + (Final × %60) ≥ 60" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
